<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<%= link_to "All articles", articles_path %> | <%= link_to "Edit", edit_article_path(@article) %> | <%= link_to "Destroy", article_path(@article), data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure you want to delete this article?"
} %>
